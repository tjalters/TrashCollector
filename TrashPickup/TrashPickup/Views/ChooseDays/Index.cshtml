@model IEnumerable<TrashPickup.Models.ChooseDay>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.DayOfWeek.DayOfWeek)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.DayOfWeek.DayOfWeek)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ChooseDayId }) |
            @Html.ActionLink("Details", "Details", new { id=item.ChooseDayId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ChooseDayId })
        </td>
    </tr>
}

</table>
